import React, { Component, PropTypes } from 'react'

class Movie extends Component {
    static propTypes = {

    };

    render() {
        return (
            <div>Фильм</div>
        )
    }
}

export default Movie