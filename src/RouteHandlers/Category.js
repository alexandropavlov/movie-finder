import React, { Component, PropTypes } from 'react'

class Category extends Component {
    static propTypes = {

    };

    render() {
        return (
            <div>Категория</div>
        )
    }
}

export default Category