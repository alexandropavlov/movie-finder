import React, { Component, PropTypes } from 'react'

class Search extends Component {
    static propTypes = {

    };

    render() {
        return (
            <div>Поиск</div>
        )
    }
}

export default Search